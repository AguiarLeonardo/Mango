import 'package:flutter/material.dart';

class BusinessPacksScreen extends StatelessWidget {
  const BusinessPacksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Business Packs Screen (Placeholder)'),
    );
  }
}
